#include <stdio.h>
#include "math/math.h"

int main(int argc,char **argv)
{
    // int a,b;
    // double m;
    // double x;
    // printf("Enter the degrees: ");
    // scanf("%lf", &m);
    // printf("sin(%0.2lf):%lf\n",m,sin(m));
    // printf("cos(%0.2lf):%lf\n",m,cos(m));
    printf("%f\n", pow(5, -3));
    return 0;
}